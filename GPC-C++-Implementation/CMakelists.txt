cmake_minimum_required(VERSION 3.15)
project(matrix CXX)

ADD_DEFINITIONS(-D__CMAKE__)
INCLUDE_DIRECTORIES("${PROJECT_SOURCE_DIR}" "${PROJECT_SOURCE_DIR}/src")
ADD_SUBDIRECTORY(example)
AUX_SOURCE_DIRECTORY(./src SRCS)
ADD_LIBRARY(mathlib ${SRCS})
